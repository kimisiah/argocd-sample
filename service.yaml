apiVersion: v1
kind: Service
metadata:
  labels:
    app: ss-ocp-dev-nginx
  name: ss-ocp-dev-nginx
  namespace: ss-ocp-dev-nginx
spec:
  ports:
  - name: nginx
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: ss-ocp-dev-nginx
  sessionAffinity: None
  type: ClusterIP
